import React, { Component } from 'react';
import './styles.scss';

const Registration = () => {
    return (
        <section>
            <h1>REGISTRATION</h1>
        </section>
    );
};

export default Registration;
