import React from 'react';
import Directory from '../../components/directory/directory';

const Home = () => {
    return (
        <section>
            <Directory />
        </section>
    );
};

export default Home;